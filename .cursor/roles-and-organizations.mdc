---
description: "Order Forge Supplier 1 : Many Traders role and organization model"
alwaysApply: true
---

Core architecture rule – Supplier 1 : Many Traders:

Roles (Keycloak Realm Roles):
- SupplierAdmin → Full access: manage products, all trader companies, users, pricing, global settings
- SupplierViewer → Read-only across the entire portal
- Trader → Browse products with own negotiated pricing, place orders, view own history
- CompanyAdmin → Manage users and addresses within their own trader company only
- OrderApprover → (Phase 2) Approve large orders within their company

Organizations / Groups:
- Each Trader Company = one Keycloak Organization (or Group)
- Supplier users have no Organization membership (only Supplier* roles)
- Every query and business operation must respect tenancy:
  - SupplierAdmin sees everything (no filter)
  - Trader / CompanyAdmin users are filtered by their organization_id / company_id

In EF Core / PostgreSQL:
- Link Keycloak user ID + organization membership to our Company entity
- Apply row-level filtering automatically based on claims
- Trader-specific pricing and orders must always be scoped to the user's Company

When generating code:
- Always enforce least privilege
- Never allow a Trader user to see another company's data
- Use claims transformation to inject company context into ClaimsPrincipal
