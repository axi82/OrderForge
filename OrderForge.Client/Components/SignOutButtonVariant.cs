namespace OrderForge.Client.Components;

public enum SignOutButtonVariant
{
    PrimaryHeaderToolbar,

    PrimaryHeaderToolbarLeadingMargin,

    PrimaryHeaderMobileFullWidth,

    /// <summary>For headers that are not the primary green bar (e.g. minimal shell).</summary>
    NeutralToolbar,
}
